#!/usr/bin/env bash
# Gracefully reloads Gunicorn.

# Find the process ID of the Gunicorn master process
MASTER_PID=$(ps aux | pgrep '[g]unicorn' | awk '{print $2}')

# Reload Gunicorn in a graceful way
kill -HUP "$MASTER_PID"

